{
  "name": "salla-webhook-server",
  "version": "1.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check gclidStore.js && node --check redis.js && node --check setup-sgtm.js",
    "test": "node --test test/run.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2",
    "redis": "^5.8.2"
  }
}
